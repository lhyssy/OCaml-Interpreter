int main(){
    // testing about constant folding and so

    int x = 100;
    int alpha = 50;

    while(x > 0){
        x = x - 1;
        alpha = alpha + 2;
    }

    return alpha * x; // 这里不应该被消除
}